/**
 * Constantes para a engine de áudio (Web Audio API).
 * Referência: Sports Sounds Pro – decode → process → render.
 */

export const DEFAULT_FFT_SIZE = 2048;
export const DEFAULT_WAVEFORM_WIDTH = 600;
export const DEFAULT_FADE_DURATION_SEC = 2;
